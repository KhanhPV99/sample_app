<li>
  <%= gravatar_for user %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to t("pages.edit_user.delete"), user, method: :delete,
      data: {confirm: t("pages.edit_user.you_sure")} %>
  <% end %>
</li>
